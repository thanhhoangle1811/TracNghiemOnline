<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

	<!-- enable use-expressions -->
	<http auto-config="true">
		<intercept-url pattern="/superadmin**" access="ROLE_SUPER_ADMIN" />
		<intercept-url pattern="/admin**" access="ROLE_SUPER_ADMIN, ROLE_ADMIN" />
		<intercept-url pattern="/employee**"
			access="ROLE_SUPER_ADMIN, ROLE_ADMIN, ROLE_EMPLOYEE" />
			
		<access-denied-handler error-page="/main/403.html" />
		<form-login login-page="/general/login.html" 
			default-target-url="/general/welcome.html"
			authentication-failure-url="/general/login.html?error"
			username-parameter="username" 
			password-parameter="password" />
		<logout logout-success-url="/general/login.html?logout" />
		
	</http>
	
	<beans:bean id="passwordEncoder" 
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" >
            <beans:constructor-arg name="strength" value="12" />
    </beans:bean>

	<!-- Select users and user_roles from database -->
	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="
		     SELECT USERNAME, PASSWORD, CASE ENABLED WHEN 1 THEN 'true' ELSE 'false' END 'ENABLED' 
		     FROM USERS 
		     WHERE USERNAME=?;"
				authorities-by-username-query="
		    SELECT u.USERNAME, r.NAME 
			FROM USERS u, USER_ROLE ur, ROLES r
			WHERE u.ID = ur.USERID and r.ID=ur.ROLEID
			AND u.USERNAME=? and ur.ENABLE=1;" />
			
			<password-encoder ref="passwordEncoder" />
			
		</authentication-provider>
	
	</authentication-manager>

</beans:beans>